version: 2

project_name: dishwatch

before:
  hooks:
    - go mod tidy

builds:
  - id: dishwatch
    main: .
    binary: dishwatch
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}}

archives:
  - id: dishwatch
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"

brews:
  - name: dishwatch
    repository:
      owner: faeton
      name: homebrew-tap
      branch: main
    directory: Formula
    homepage: https://github.com/faeton/dishwatch
    description: Starlink dish status, dashboard, and power-bank CLI — talks to the local gRPC API (no auth required)
    license: MIT
    commit_author:
      name: goreleaser
      email: faeton@gmail.com
    install: |
      bin.install "dishwatch"
      bin.install_symlink "dishwatch" => "sl"
    test: |
      system "#{bin}/dishwatch", "--help"
      system "#{bin}/sl", "--help"

release:
  github:
    owner: faeton
    name: dishwatch
  draft: false
  prerelease: auto
  # The Mac app is not built by goreleaser — it needs Xcode, a Developer ID and
  # two notary round-trips — so it is attached rather than produced here. Build
  # it AFTER tagging: the bundle version comes from `git describe`, so a DMG
  # built before the tag carries the previous version in its name, its
  # Info.plist and its notarization.
  extra_files:
    - glob: app/.build/DishWatch-*.dmg
